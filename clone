clone is a git command that allows you to copy an existing repository.

Is a shortcut for several commands: 
	it creates a folder and move into it (mkdir, cd), 
	initialize the folder (git init), 
	adds a remote repo (git remote) 
	and pulls the code from github.
